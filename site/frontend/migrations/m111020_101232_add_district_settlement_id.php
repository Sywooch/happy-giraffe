<?php
class m111020_101232_add_district_settlement_id extends CDbMigration
{
	public function up()
	{
		$this->execute("ALTER TABLE `geo_rus_district`
ADD COLUMN `settlement_id` INT (11)");

$this->execute("REPLACE INTO `geo_rus_district` (`id`, `name`, `region_id`, `settlement_id`) VALUES
	(1871, 'Москва', 42, 148315),
	(1872, 'Санкт-Петербург', 59, 148316);");
		
		if(Yii::app()->hasComponent('cache'))
		{
			Yii::app()->getComponent('cache')->flush();
			echo "Cache flused\n";
		}
		
		$this->clearAssets();
	}
	
	private function clearAssets()
	{
		$path = Yii::app()->getComponent('assetManager')->getBasePath();
		$this->clearDir($path);
		echo "Assets clear\n";
	}

	private function clearDir($folder, $main=true)
	{
		if(is_dir($folder))
		{
			$handle = opendir($folder);
			while($subfile = readdir($handle))
			{
				if($subfile == '.' || $subfile == '..')
					continue;
				if(is_file($subfile))
					unlink("{$folder}/{$subfile}");
				else
					$this->clearDir("{$folder}/{$subfile}", false);
			}
			closedir($handle);
			if(!$main)
				rmdir($folder);
		}
		else
			unlink($folder);
	}

	public function down()
	{
		echo "m111020_101232_add_district_settlement_id does not support migration down.\n";
		return false;
		
		$this->execute("");
	}

	/*
	// Use safeUp/safeDown to do migration with transaction
	public function safeUp()
	{
	}

	public function safeDown()
	{
	}
	*/
}
